/*
 * Em matemática, matriz transposta é a matriz que se obtém da troca de linha
 * as por colunas de uma dada matriz. Desta forma, transpor uma matriz é a
 * operação que leva na obtenção de sua transposta.  
 *
 * Escreva um programa que, dada uma matriz, gere a sua matriz transposta .
 */

int main() {
    return 0;
}
