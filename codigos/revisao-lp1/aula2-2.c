#include <stdio.h>

int main() {

  int x, y, z;

  /* cálculo */
  if (x > 1010) {
    printf("Foi identificado uma anomalia em X");
  }
  if(y < -100) {
    printf("Foi encontrado uma anomalia em Y");
  }

  /* cálculo */
  if (x > 1010) {
    printf("Foi identificado uma anomalia em X");
  }
  if(y < -100) {
    printf("Foi encontrado uma anomalia em Y");
  }

  /* código */
  if (x > 1010) {
    printf("Foi identificado uma anomalia em X");
  }
  if(y < -100) {
    printf("Foi encontrado uma anomalia em Y");
  }
}
